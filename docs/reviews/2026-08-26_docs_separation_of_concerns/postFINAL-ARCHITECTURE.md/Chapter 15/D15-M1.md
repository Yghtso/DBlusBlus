## D15-M1 verdict

**RESOLVED AND INTEGRATED.** F15-Q1 is **CLOSED**.

Chapter 5 now canonically requires:

```text
xmax == INVALID_TXN_ID  =>  cmax == 0
```

The edits are in [docs/ARCHITECTURE.md](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:3154).

## Git state

Initial state:

- HEAD: `32fe8712d9e0995dc4a27e06089ccf8e4333916c`
- Tracked working tree: clean
- Index: clean
- Pre-existing untracked review artifact:
  `docs/reviews/2026-08-26_docs_separation_of_concerns/postFINAL-ARCHITECTURE.md/Chapter 15/D15-M1.md`

The artifact was not read, modified, moved, or staged.

Final state:

- HEAD unchanged: `32fe8712d9e0995dc4a27e06089ccf8e4333916c`
- Modified: `docs/ARCHITECTURE.md`
- Same pre-existing untracked review artifact
- Index: clean
- `git diff --check`: passed
- No external tracked change was detected during the task.

## Architecture integration

The original ambiguity allowed arbitrary persisted `cmax` bytes when no deleter existed. The accepted rule now establishes one byte-canonical representation.

- Chapter-5 generic owner: [§5.7.3](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:3154)
- HEAP L1 validation: [§4.13.3](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:2353)
- Consolidated invariant: [§5.21](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:3866)
- INSERT consumer: [§15.2](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:12093)
- UPDATE consumer: [§15.3](/home/yghtso/Projects/DBlusBlus/docs/ARCHITECTURE.md:12134)

A recognized-v1 persisted tuple with invalid `xmax` and nonzero `cmax` is now explicitly `CORRUPT_HEAP`. Ordinary reads neither normalize it nor treat it as a future-format encoding.

`CommandId{0}` remains legal. With invalid `xmax`, `cmax=0` means “semantically inactive canonical field,” not “deleted by command 0.” No new CommandId sentinel was introduced.

## Resulting tuple headers

INSERT:

```text
xmin = current TxnId
xmax = INVALID_TXN_ID
cmin = current CommandId
cmax = 0
prev = canonical no-previous-version pair
```

UPDATE replacement version:

```text
xmin = current TxnId
xmax = INVALID_TXN_ID
cmin = current CommandId
cmax = 0
prev = old RID
```

Unchanged UPDATE old version and DELETE:

```text
xmax = current TxnId
cmax = current CommandId
```

Chapter 14 already normalized aborted deleters to:

```text
xmax = INVALID_TXN_ID
cmax = 0
```

Therefore no Chapter-14 hunk was necessary. The generic rule also applies to catalog/system tuples using the same header.

## Regression assessment

Unchanged:

- 48-byte tuple-header size, field widths, and offsets
- format and schema versions
- no-deleter visibility
- SELF deleter comparisons
- creator `cmin` and FROZEN semantics
- WAL record grammar and checksum coverage
- D14-M1, D14-M2, and D14-M3
- statement, CommandId, retry, uniqueness, locking, revalidation, and failure-state semantics
- no physical user-DML undo rules

Positive cases are now exact:

- INSERT command 0: `(xmin=T, xmax=0, cmin=0, cmax=0)`
- INSERT command 7: `(xmin=T, xmax=0, cmin=7, cmax=0)`
- UPDATE replacement command 9: `(xmin=T, xmax=0, cmin=9, cmax=0, prev=old RID)`
- Actual self-delete command 9 remains `(xmax=T, cmax=9)`

Invalid recognized-v1 states include invalid `xmax` with `cmax=1`, `UINT32_MAX`, or stale prior command bytes. All classify as `CORRUPT_HEAP`.

## Deferred scope

- F15-Q2: **FROZEN / OPEN / untouched**
- F15-3: pending, untouched
- F15-4: pending, untouched
- F15-5: pending, untouched
- F15-6: pending, untouched
- Chapter-15 verification: pending
- Chapter-16 review: not started
- Chapter-15 architecture: **NOT CLEAN / NOT CLOSED**

The decision unblocks later byte-exact verification for fresh INSERT headers, UPDATE replacement headers, malformed invalid-`xmax`/nonzero-`cmax` headers, and aborted-`xmax` normalization. `VERIFICATION.md` was not edited.

## Re-read results

Questions 1–33: **YES**.

- 34: **YES** — F15-Q2 remains untouched, frozen, and open.
- 35–38: **YES** — F15-3 through F15-6 remain untouched.
- 39–44: **NO** — no Chapter 16, §31.7, Appendix C, verification-procedure, chronology, or source-layout change.
- 45–46: **YES** — fresh headers are byte-canonical and verification can derive one exact value.
- 47: **NO** — no new semantic question arose.

Documentation-model assessment:

- Chronology/current-state/DEVELOPMENT/VERIFICATION/PROJECT_STATE/history leakage: **NO**
- Persistent-format precision, ownership clarity, analytical sufficiency, implementation freedom, and timelessness: **YES**

Cross-chapter consistency is preserved across Chapters 5, 9, 10, 12, 14, and 15.

## Diff classification

Task-created logical hunks:

- A — Chapter-5 generic invariant
- B — Chapter-5 validation/corruption synchronization
- D — INSERT fresh-header synchronization
- E — UPDATE replacement-header synchronization
- F — Chapter-5 invariant-summary synchronization
- G — precise §5.7.4 predecessor cross-reference
- H — required invariant-list renumbering

No C hunk was needed because normalization was already canonical.

Only `docs/ARCHITECTURE.md` was task-modified. No implementation work, build, tests, benchmarks, staging, commit, devlog, or review artifact occurred. Phase 2 remains **NOT STARTED / NOT AUTHORIZED**.
