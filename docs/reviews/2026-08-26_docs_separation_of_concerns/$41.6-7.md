Expanded [docs/VERIFICATION.md](/home/yghtso/Projects/DBlusBlus/docs/VERIFICATION.md:2072) to operationalize all current §41.6–§41.7 obligations.

1. **Sections added:** statistics algorithms; publication/versioning; persistence/validation; semantic emptiness; physical properties; memo/pruning; cost model; determinism/resource limits; final optimizer validation; diagnostics.
2. **Sections modified:** statistics, selectivity, join estimation, access paths, join order, memory/spill planning, differential tests, fuzzing, and plan regressions.
3. **Statistics algorithms:** controlled exact and approximate collection procedures now cover construction, serialization, normalization, and reproducibility.
4. **HLL:** covers empty through large domains, duplicates, canonical hashing, register constraints, error distributions, and optional merge behavior.
5. **MCV/heavy hitters:** covers dominant, near-cutoff, uniform, NULL-heavy, and duplicate-heavy distributions with bounded-error and canonical-order assertions.
6. **Reservoir/histogram:** covers deterministic sampling, degenerate inputs, bucket limits, boundary ordering, mass, and MCV exclusion.
7. **NULL/mass:** verifies NULL-excluded NDV, separate NULL fraction, residual population, and no MCV/histogram double counting.
8. **Statistics ordering:** verifies binary VARCHAR and canonical FLOAT64 ordering without locale or raw-memory comparisons.
9. **ANALYZE snapshot:** uses concurrent DML barriers to verify one stable SQL visibility snapshot.
10. **StatsVersion:** verifies exact `(TxnId, CommandId)` identity, single ownership, consistent rows/chunks, and version ordering.
11. **Manifest/completeness:** covers missing, duplicate, reordered, corrupt, wrong-version, and unlisted members.
12. **Publication/abort:** only complete terminal-COMMITTED generations publish globally; aborted and partial candidates remain invisible.
13. **Post-commit cache failure:** preserves COMMITTED and permits only the architecture-approved older/missing safe fallback.
14. **Corrupt/incomplete generations:** validates framing, CRC, versions, scalar codecs, identities, numerical fields, and generation-atomic rejection.
15. **Normalization:** verifies canonical scalar identities, positive zero, exact-derived residuals, normalized bin mass, and round-trip equivalence.
16. **Historical generations:** selects the newest visible committed complete valid generation, with older/missing fallback.
17. **Status reclamation:** verifies StatsVersion remains usable without retaining or querying creator transaction status.
18. **Logical versus physical population:** separates visible-row selectivity from heap/index garbage and physical-pressure statistics.
19. **Selectivity:** covers equality, all registered inequalities, normalized bound conjunctions, IN, NULL predicates, AND, OR, and NOT.
20. **NULL/3VL selectivity:** requires complete TRUE/FALSE/UNKNOWN triples, including NULL-bearing IN lists.
21. **Same-column predicates:** covers overlap, nesting, touching bounds, exact contradictions, and equality/range interaction.
22. **Q-error distinction:** estimate quality remains separate from semantic correctness and proof authority.
23. **Semantic emptiness:** added a dedicated cross-layer estimator/planner/memo/validator/execution procedure.
24. **Estimated-zero regression:** numerical zero retains executable alternatives unless an approved proof exists.
25. **Stale zero after insert:** ANALYZE-empty followed by a committed insert must still execute and return the row.
26. **Exact logical proofs:** covers constant FALSE/UNKNOWN, exact contradictions, zero-row Values, NOT NULL, and SQL `LIMIT 0`.
27. **LEFT JOIN emptiness:** empty right/no-TRUE ON preserves and null-extends left rows.
28. **Aggregate emptiness:** grouped aggregation propagates empty-child proof; global aggregation still produces one row.
29. **Stale domains:** min/max, histogram, MCV, HLL, and disjoint-domain evidence cannot prove absence.
30. **LIMIT/objective zero:** distinguishes SQL `LIMIT 0` from `FIRST_K_ROWS(0)`.
31. **Base access:** covers cardinality, physical size, width, correlation, pressure, cache assumptions, costs, and ordering.
32. **Physical pressure/cache:** equal logical populations may produce different physical costs without changing semantics.
33. **No fixed threshold:** same selectivity can choose different paths when other cost inputs differ.
34. **Composite bounds:** covers equality prefixes, one range, inclusivity, RID bounds, `IS NULL`, `= NULL`, suffixes, and residuals.
35. **Join estimation:** preserves unique-to-many, many-to-many, skew, overlap, NULL, and MCV fixtures.
36. **LEFT JOIN lower bound:** estimated output cannot fall below the preserved-left estimate unless the left input is proven empty.
37. **Cartesian legality:** covers required disconnected products and avoidance of unnecessary products.
38. **LEFT JOIN reorder:** permits inner reordering within sides but rejects crossing the outer-join boundary.
39. **Hash orientation:** tests both supported INNER orientations and the fixed LEFT orientation.
40. **INLJ:** covers selective small-outers and large/unselective losing cases.
41. **Interesting orders:** preserves ordered alternatives when they reduce total downstream cost.
42. **Sort enforcement:** requires Sort only when no exact property provider exists and includes its full cost.
43. **Top-N:** compares small/large K, no LIMIT, incompatible ordering, and memory effects.
44. **Aggregate choices:** compares hash and capability-enabled ordered aggregation with enforcement included.
45. **DISTINCT choices:** analogously compares hash and ordered implementations.
46. **Memory-budget planning:** identical logical inputs may choose different legal spill-aware plans.
47. **Full-result/first-K:** tests distinct startup and total-cost objectives and memo identity.
48. **Missing/stale statistics:** requires finite deterministic fallback, explicit confidence/provenance, and no fabricated proof.
49. **Deterministic ties:** perturbs insertion order, allocation, and hash iteration while requiring stable selection.
50. **Pointer/hash nondeterminism:** explicitly forbidden in memo retention and final plan choice.
51. **PlanFingerprint:** tests diagnostic use and simulated collision without treating the hash as identity.
52. **Bounded fallback:** covers thresholds, arena/work limits, deterministic heuristic transition, and controlled resource failure.
53. **Memo/pruning:** verifies semantic validity, required properties, low-startup alternatives, and proof state survive dominance.
54. **Estimated-zero memo regression:** executable zero-estimate alternatives remain available and are differentially executed.
55. **Finite/nonnegative costs:** covers scans, joins, Sort, Top-N, aggregates, DISTINCT, materialization, and spills.
56. **Payload width:** verifies pruning changes decode, memory, hash, sort, and spill costs.
57. **Memory/spill costs:** verifies deterministic target assignment, simultaneous blocker accounting, and spill response.
58. **Property-enforcement cost:** required enforcers are included before alternatives are compared.
59. **Stable optimizer statistics:** one optimization retains generation A while concurrently published B applies only later.
60. **Final validation:** checks semantics, slots, ordering, joins, capability gates, memory declarations, and proof provenance.
61. **Malformed final plans:** even the numerically cheapest invalid plan is rejected before execution or side effects.
62. **Stats-derived proof rejection:** explicitly rejects numerical-zero statistics promoted to semantic emptiness.
63. **Final properties:** validates output slots, hidden RIDs, join semantics, and required/provided ordering.
64. **Differential correctness:** preserved and expanded for NULLs, skew, stale statistics, zero estimates, algorithms, and memory changes.
65. **Optimizer fuzzing:** preserved and expanded for slots, proof metadata, budgets, bounded failure, and reproducible seeds.
66. **Plan regressions:** preserved and consolidated into one capability-focused section.
67. **Diagnostics:** covers StatsVersion, provenance, proof state, access/join choices, costs, properties, pruning, fallback, and resources.
68. **Architecture references:** added targeted references to Chapters 20 and 34–38 plus existing §41.3/§41.5 procedures.
69. **Not duplicated:** persisted layouts, exact formulas, proof whitelist, memo algorithms, cost equations, and physical validator matrix remain architecture-owned.
70. **S-001–S-037:** COMPLETE 37; PARTIAL 0; MISSING 0; CONTRADICTORY 0.
71. **O-001–O-033:** COMPLETE 33; PARTIAL 0; MISSING 0; CONTRADICTORY 0.
72. **Remaining §41.6 gaps:** none identified.
73. **Remaining §41.7 gaps:** none identified.
74. **§41.4:** parser, binder, catalog, subquery, and logical-validator gaps remain untouched.
75. **§41.3:** not substantively rewritten; only a post-durable COMMIT cross-reference was added.
76. **§41.5:** not substantively rewritten; only validator, pre-DML, and EXPLAIN ANALYZE cross-references were added.
77. **Architecture questions:** none. Direct `BETWEEN` dispatch remains unsupported; normalized registered bounds are tested instead.
78. **Time independence:** no dates, status narration, run totals, machine versions, or milestone history introduced.
79. **Files changed:** only `docs/VERIFICATION.md`.
80. **`git diff --check`:** passed with no output.
81. **ARCHITECTURE:** unchanged.
82. **DEVELOPMENT:** unchanged.
83. **PROJECT_STATE:** unchanged.
84. **README:** unchanged.
85. **AGENTS:** unchanged.
86. **Source/tests/build configuration:** unchanged; no build or tests were run for this documentation-only task.